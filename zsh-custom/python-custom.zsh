#!/usr/bin/env zsh
# I don't like using anaconda, but I'm keepting these for reference.

# This is the old way
# export PATH=/usr/local/anaconda3/bin:"${PATH}"

# This is the new way
#. "/usr/local/anaconda3/etc/profile.d/conda.sh"
#conda activate
